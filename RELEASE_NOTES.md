# Eslabong 한국어 보완 · 유물 프리셋 v1.2.1

게임 업데이트 후 작동하지 않던 유물 프리셋을 수정했습니다.

- 주전 저장·불러오기 호출 관계를 분석하도록 유물 연결 코드를 교체했습니다. 화면 함수명·지역변수명이 바뀌거나 저장 데이터가 별도 변수로 옮겨져도 같은 흐름을 찾아 연결합니다.
- 공개용과 개인용 실행기의 유물 처리를 공통화하고 중복 설치 단계를 제거했습니다.
- 게임의 장착·해제 처리를 그대로 사용합니다. 함수 내용이 달라졌다는 이유만으로 설치를 거절하지 않으며, 호출할 수 없는 API나 모호한 연결은 변경 전에 중단합니다.
- 이전 설치 창을 닫고 이번 압축의 실행 파일을 사용하세요. 실행 중인 구형 설치기가 새 코드로 자동 교체되지는 않습니다.

- 현재 게임의 주전 저장·불러오기 위치를 다시 찾아 연결하도록 개선했습니다.
- 기존 장착 함수로 소유한 유물만 이동하며, 실패 시 배치·품질·소유 목록을 되돌리는 검사를 강화했습니다.
- **적용 후 게임 실행** 버튼을 추가했습니다. 이 실행기로 게임을 켜면 업데이트 후에도 현재 데이터에 다시 적용합니다.
- 한국어 보완과 기존 번역 재사용 기능을 유지합니다. 새 원문을 추측해서 번역하거나 새 한국어를 덮어쓰지 않습니다.

이전 버전의 보완 내용도 포함합니다.

- 시장·선수단 카드의 새 이름 표시 경로에 대응했습니다. Shiny 식별 표시는 유지합니다.
- 전투 HUD, 도전의 탑, 기본 환생의 성장 재설정, 외형 편집, 미라주 관련 문구 등 125개 항목을 GPT가 직접 번역·보완했습니다.
- 기존 번역 재사용을 개선했습니다. 번역 키 이동·공백·줄바꿈 변경에 대응하고, 화면 함수의 일부가 바뀌어도 일치하는 표시 구문을 따로 적용합니다. 새 한국어와 다른 언어 데이터는 보존합니다.
- 변경된 주전 확정 함수에 유물 프리셋을 연결했습니다. 보유한 개별 유물만 이동하며, 없는 유물을 복사하거나 생성하지 않습니다.
- 설치 경로 자동 탐색에서 개발 PC 전용 경로를 제거했습니다.

## 설치

첨부된 **Eslabong-Korean-Relic-Mods.zip**을 받으세요. GitHub의 `Source code` ZIP은 설치용이 아닙니다.

1. 게임을 저장하고 완전히 종료합니다.
2. 압축을 원하는 폴더에 풀고 `EslabongMods.exe`를 실행합니다.
3. `eslabong.exe`와 `eslabong.pck`가 있는 폴더를 선택합니다.
4. 원하는 모드를 체크하고 **적용 후 게임 실행**을 누릅니다. 설치만 하려면 **선택한 구성 적용**을 사용합니다.

기존 모드를 먼저 제거할 필요는 없습니다. Windows 64비트용이며 Python·별도 명령 실행·추가 글꼴 설치가 필요하지 않습니다. 변경 직전 게임 데이터를 백업하며, 저장 파일은 수정하지 않습니다.

Steam이나 게임 EXE로 직접 실행하면 업데이트 복구는 실행되지 않습니다. 백그라운드 자동 감시는 없으며, 필수 연결 구조가 크게 바뀌어 안전하게 적용할 수 없을 때는 파일을 변경하지 않고 중단합니다.

유물은 **선수단 → 주전 저장 → 유물도 함께 저장·불러오기**에서 사용합니다. 기존 슬롯은 체크한 상태로 한 번 덮어써야 유물 배치가 기록됩니다.

이 공개 배포에는 한국어 보완·유물 프리셋만 포함됩니다. 게임 본체·원본 스크립트 전체·개인 저장 파일·복호화 키는 포함하지 않습니다. 격리된 엔진과 설치기 테스트를 사용했으며, 실제 플레이 화면 전체를 검증했다는 뜻은 아닙니다.
