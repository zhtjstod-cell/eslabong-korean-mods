# Eslabong 한국어 보완 · 유물 프리셋 v1.0.1

## 수정 사항

- 업데이트 후 `MercenaryInstance.gdc` 불일치로 설치가 중단되던 문제를 수정했습니다.
- 스크롤형으로 변경된 주전 저장 창에도 유물 체크박스를 추가합니다. 게임의 새 창 크기 조절은 유지합니다.
- 파일 전체가 달라졌을 때 필요한 함수·표시 구문을 자동으로 찾아 적용하고, 다른 게임 변경은 보존합니다.
- 모드를 제거할 때 게임에 원래 포함된 한국어까지 삭제하지 않도록 복구 기록을 추가했습니다.
- 구형 데이터, 업데이트된 클린 데이터, 새 함수·줄 번호·팝업 표시가 달라진 테스트 데이터에서 설치·재적용·제거를 확인했습니다.

게임 업데이트로 모드가 풀리면 **이 설치기를 다시 실행해 적용**하세요. 필요한 부분이 일치하는 업데이트는 새 배포를 기다리지 않고 처리합니다. 새 문장을 자동 번역하는 기능은 아닙니다. 유물 장착·저장 규칙이 달라 안전을 확인할 수 없을 때는 해당 기능 설치를 중단합니다.

## 다운로드 / 설치

아래 첨부 파일에서 **Eslabong-Korean-Relic-Mods.zip**을 받으세요. `Source code` 압축 파일은 설치용이 아닙니다.

1. 게임을 저장하고 완전히 종료합니다.
2. ZIP을 원하는 폴더에 풀고 `EslabongMods.exe`를 실행합니다.
3. `eslabong.exe`가 있는 게임 폴더를 선택합니다.
4. 원하는 모드를 체크하고 **선택한 구성 적용**을 누릅니다.

Windows 64비트용입니다. Python이나 PowerShell 명령을 따로 실행할 필요가 없습니다. 두 모드를 각각 선택해 설치·제거할 수 있으며, 변경 전 게임 데이터 전체를 백업합니다.

## 한국어 보완

- 누락 번역과 영어로 남던 설명문을 보완했습니다.
- 기존 캐릭터와 앞으로 기본 목록에서 생성되는 캐릭터의 이름·별칭을 한국어에서 한글로 표시합니다.
- 한국어에서는 직업 이름도 한글로 표시합니다.
- 보완 번역과 음역은 GPT가 직접 작성한 기존 작업을 사용합니다. 원래 있던 한국어 전체를 새로 번역한 것은 아닙니다.

## 유물 프리셋

**선수단 → 주전 저장 → ‘유물도 함께 저장·불러오기’**를 체크합니다.

- 저장한 인물에게 보유 중인 개별 유물을 다시 장착합니다. 다른 인물이 착용하고 있으면 옮깁니다.
- 고유 ID로 관리하므로 같은 종류의 다른 물건을 대신 만들거나 복사하지 않습니다.
- 판매·분실한 유물과 장착할 수 없는 항목은 건너뜁니다.
- 적용 중 규칙 충돌이 생기면 유물 변경을 원래 상태로 돌립니다.

**기존 슬롯은 유물 기록이 없으므로, 체크한 상태로 한 번 덮어써 주세요.** 유물 포함 불러오기에는 선수 배치 전용 ‘되돌리기’ 버튼을 제공하지 않습니다.

## 참고

- 비공식 설치형 모드입니다. `mods` 폴더에 넣기만 하는 방식이 아닙니다.
- 이미 문장으로 저장된 옛 뉴스나 사용자 제작 이름까지 모두 다시 번역하지는 않습니다.
- 유물 관련 2,164개 검사, 1,000가지 장착 순열, 이름·언어 전환 및 자동 매칭 설치·제거 검사를 통과했습니다. 실제 플레이 화면의 시각 검증은 별도입니다.
- 게임 본체, 원본 스크립트 전체, 저장 파일, 복호화 키는 포함하지 않습니다.

자세한 사용법은 ZIP 안의 README 또는 저장소의 안내를 참고하세요.
